Whenever I ask to modify a section, first identify the CSS class, HTML structure, and any related JavaScript responsible for that element.

Before making changes, tell me:

- HTML file and line (if available)
- CSS selector(s) controlling it
- JavaScript affecting it (if any)
- Responsive breakpoints that modify it
- Parent container affecting its layout
- Related utility classes

Then make the requested changes.

Never edit unrelated sections.

If multiple selectors affect the same element, explain which one has the highest priority.